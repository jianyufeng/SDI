package com.puyu.mobile.sdi.model;


import com.puyu.mobile.sdi.mvvm.model.DataRepository;

public class PressurizeRepository extends DataRepository {
    public PressurizeRepository() {
        super();
    }

    /*public Observable<UserData> login(String account, String password) {
        Map<String, Object> map = new HashMap<>();
        map.put("username", account);
        map.put("password", password);
//        map.put("appType", 2);
        return api.login(map).compose(new ObjTransform<>(null));
    }*/

    /*public Observable<UserData> getUserMsg() {
        return api.getUserData().compose(new ObjTransform<>(null));
    }*/

}
