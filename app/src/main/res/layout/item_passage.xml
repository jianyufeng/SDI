<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.puyu.mobile.sdi.bean.PassageBean" />

        <variable

            name="pos"
            type="java.lang.Integer" />
        <variable
            name="adapter"
            type="com.puyu.mobile.sdi.frag.StandardGasConfigFrag.PassageAdapter" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:descendantFocusability="blocksDescendants"
        android:onClick="@{(view)->adapter.itemClick(view,viewModel,pos)}">

        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/checkbox"
            android:layout_width="@dimen/dp26"
            android:layout_height="0dp"
            android:buttonTint="@color/c_16a5ff"
            android:checked="@={viewModel.pass1Swich}"
            android:focusable="false"
            android:gravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="@dimen/dp10"
            android:text="@{viewModel.name}"
            android:textColor="@color/c_384051"
            android:textSize="@dimen/sp14"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toRightOf="@id/checkbox"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/show_flag"
            android:layout_width="@dimen/dp4"
            android:layout_height="0dp"
            android:background="@color/c_16a5ff"
            android:visibility="invisible"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>